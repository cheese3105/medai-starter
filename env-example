REASONING_MODEL=gemini-3.5-flash-lite
REASONING_MODEL_BASE_URL=https://generativelanguage.googleapis.com/v1beta/openai
REASONING_MODEL_API_KEY=AQ.Ab8RN6KpPkX-vQ-qVZVfvg2c70-2pihadDXgwE5HVy9LLI28lw

# --- Optional (v2) - Verifier Agent dùng model/provider riêng ---
# Không set 3 dòng này -> Verifier tự fallback dùng chung
# REASONING_MODEL_BASE_URL/REASONING_MODEL_API_KEY ở trên (giống hành vi cũ).
# Set nếu muốn verifier gọi 1 provider khác (VD model rẻ hơn để verify).
VERIFIER_MODEL=
VERIFIER_MODEL_BASE_URL=
VERIFIER_MODEL_API_KEY=

# --- Optional (v2) - bước tự đánh giá "evidence đã đủ chưa" của Retrieval
# Agent dùng model/provider riêng ---
# Chỉ có tác dụng khi retrieval.max_iterations > 1 trong config YAML.
# Không set -> tự fallback dùng chung REASONING_MODEL_BASE_URL/API_KEY.
RETRIEVAL_CHECK_MODEL=
RETRIEVAL_CHECK_MODEL_BASE_URL=
RETRIEVAL_CHECK_MODEL_API_KEY=

# Cần cho retrieval/ (v1 - RAG). Theo implementation-decisions.md của
# thành viên nghiên cứu RAG: dùng baai/bge-m3 qua OpenRouter làm embedding.
EMBEDDING_MODEL=baai/bge-m3
EMBEDDING_MODEL_API_BASE=https://openrouter.ai/api/v1
EMBEDDING_MODEL_API_KEY=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

